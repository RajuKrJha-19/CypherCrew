[pytest]
testpaths = tests
addopts = -q
filterwarnings =
    # The codebase uses datetime.utcnow() throughout (matched by the models'
    # default=datetime.utcnow). Deprecation is tracked separately; don't let
    # it drown the test signal.
    ignore::DeprecationWarning
