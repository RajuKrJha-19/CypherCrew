"""Google Business Profile reviews: sync, AI-drafted replies, and a guarded
opt-in auto-reply. Simulation-first (a scripted source) so the whole flow is
usable + tested before the real Reviews API is enabled.
"""
